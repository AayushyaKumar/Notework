version: '3.8'

services:
  backend:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
    env_file:
      - .env
  
    restart: unless-stopped

    dns:
      - 8.8.8.8
      - 8.8.4.4